<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HOME</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body{
            font-family:'Times New Roman', Times, serif
        }
        /* ปรับคอนเทนเนอร์ */
        .container {
            max-width: 1800px; /* กำหนดความกว้างสูงสุด */
            margin: 40px auto; /* จัดกึ่งกลางด้วย margin */
            padding: 20px; /* เพิ่ม padding ภายในคอนเทนเนอร์ */
            background-color: #f9f9f9; /* เพิ่มสีพื้นหลังอ่อนๆ */
            border-radius: 10px; /* ทำให้มุมของคอนเทนเนอร์โค้งมน */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1); /* เพิ่มเงา */
        }

        /* ปรับแต่งตาราง */
        .table-responsive {
            max-height: 400px; /* กำหนดความสูงสูงสุด */
            overflow-y: auto; /* ทำให้เลื่อนในแนวตั้ง */
            border: 1px solid #ddd; /* ขอบของตาราง */
            border-radius: 5px; /* ทำให้มุมของตารางโค้งมน */
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px; /* เพิ่มระยะห่างจากส่วนอื่นๆ */
            table-layout: auto; /* ปรับให้คอลัมน์ขยายตามขนาดเนื้อหา */
        }

        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
            white-space: nowrap; /* ไม่ให้ข้อความตัดบรรทัด */
        }

        th {
            background-color: #4CAF50; /* เปลี่ยนสีหัวตาราง */
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2; /* เพิ่มสีสลับสำหรับแต่ละแถว */
        }

        /* ปรับแต่งหัวข้อ */
        h1, h2 {
            text-align: center;
            margin-bottom: 20px;
            font-family: 'Arial', sans-serif;
        }

        /* ปรับแต่ง Navbar */
        .navbar {
            background-color: #4CAF50; /* เปลี่ยนสีพื้นหลังของ Navbar */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2); /* เพิ่มเงาให้กับ Navbar */
        }

        .navbar-brand {
            font-weight: bold; /* ทำให้ชื่อแบรนด์หนาขึ้น */
            font-size: 1.5rem; /* ขยายขนาดตัวอักษร */

        }

        .nav-link {


        }

        .nav-link:hover {
            color: #4CAF50; /* เปลี่ยนสีเมื่อเอาเมาส์ไปอยู่เหนือ */
        }

        /* ทำให้ปุ่ม LOGOUT เป็นสีแดง */
        .nav-link.logout {
            color: white; /* ให้สีข้อความเป็นขาว */
            background-color: red; /* เปลี่ยนสีพื้นหลังเป็นแดง */
            border-radius: 5px; /* ทำให้มุมโค้งมน */
            padding: 8px 12px; /* เพิ่ม padding */
            transition: background-color 0.3s; /* เพิ่มการเปลี่ยนสี */
        }

        .nav-link.logout:hover {
            background-color: darkred; /* เปลี่ยนสีเมื่อเอาเมาส์ไปอยู่เหนือ */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
            <a class="navbar-brand" href="#">ระบบเช็คชื่อ</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="#">หน้าหลัก</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/check-in">เช็คชื่อ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/data">ข้อมูลการเข้าชั้นเรียน</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link logout" href="/">LOGOUT</a> <!-- เพิ่มคลาส logout -->
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <h1>รายชื่อนักศึกษาทั้งหมด</h1>
        <div class="table-responsive">
            <table>
                <thead>
                    <tr>
                        <th>เลขที่</th>
                        <th>คำนำหน้า</th>
                        <th>ชื่อ</th>
                        <th>นามสกุล</th>
                        <th>วัน / เดือน / ปี เกิด</th>
                        <th>เพศ</th>
                        <th>หลักสูตร</th>
                        <th>สถานศึกษาก่อนหน้า</th>
                        <th>ที่อยู่</th>
                        <th>เบอร์โทรศัพท์</th>
                        <th>อีเมล</th>
                        <th>LINE_ID</th>
                        <th>สถานภาพ</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach(row => { %>
                        <tr>
                            <td><%= row.std_id %></td>
                            <td><%= row.prefix %></td>
                            <td><%= row.first_name %></td>
                            <td><%= row.last_name %></td>
                            <td><%= row.date_of_birth %></td>
                            <td><%= row.sex %></td>
                            <td><%= row.curr_name_th %></td>
                            <td><%= row.previod_school %></td>
                            <td><%= row.address %></td>
                            <td><%= row.telephone %></td>
                            <td><%= row.email %></td>
                            <td><%= row.line_id %></td>
                            <td><%= row.status %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Bootstrap JS (Optional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
